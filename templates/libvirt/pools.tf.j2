# Libvirt Storage Pools
{% for pool in tf_libvirt_pools %}

resource "libvirt_pool" "{{ pool.name }}" {
  name = "{{ pool.name }}"
  type = "{{ pool.type }}"
  path = "{{ pool.path }}"
}
{% endfor %}
